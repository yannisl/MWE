# Examples of Using gnuplot with LaTeX

The examples use the `pgfplots` package to run gnuplot commands and render charts. Best to run them using `LuaLaTeX` to make font selection easier. Run them with `-shell-escape` option enabled.

